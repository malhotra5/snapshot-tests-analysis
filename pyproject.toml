[project]
name = "snapshot-tests-analysis"
version = "0.1.0"
description = "Mining and categorization scripts for analyzing snapshot testing impact on OpenHands-CLI"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31",
]

[project.scripts]
mine-github = "scripts.mine_github:main"
categorize-prs = "scripts.categorize_prs:main"
